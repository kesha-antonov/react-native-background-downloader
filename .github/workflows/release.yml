name: Release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest

    permissions:
      contents: read
      # Required for npm provenance - lets the job mint the OIDC token that
      # npm records as a signed, verifiable link back to this workflow run.
      id-token: write

    steps:
      - uses: actions/checkout@v5

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: yarn

      - run: yarn install --immutable

      # app.plugin.js requires plugin/build/index.js, which does not exist on a
      # clean checkout, so the plugin has to be built before anything loads it.
      - name: Build the Expo config plugin
        run: yarn build-plugin

      - name: Build
        run: yarn build

      - run: yarn typecheck

      - run: yarn test

      - run: yarn lint

      # Authenticated by npm trusted publishing: the id-token permission above
      # lets npm exchange this run's OIDC token for a short-lived credential,
      # so there is no NPM_TOKEN secret to store, leak or rotate.
      - name: Publish
        run: npm publish --provenance --access public
